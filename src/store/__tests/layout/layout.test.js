
/*import { render, screen } from '@testing-library/react';

  import {Layout } from "../components";

  describe("layout", () => {
   
      it("render div", () => {
       const dom = render(<Layout/>);
       expect(dom.firstChild.classList.contains('body')).toBe(true)
     //  expect(dom.classList.contains('active')).toEqual(true) 
  });
});
  */